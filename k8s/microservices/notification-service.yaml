apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
        sidecar.istio.io/inject: "true"
    spec:
      containers:
        - name: notification-service
          image: ultrahpm/notification-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8205
          env:
            - name: SPRING_CONFIG_IMPORT
              value: "optional:configserver:http://config-server:8888"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://eureka:8761/eureka/"
            - name: SPRING_THREADS_VIRTUAL_ENABLED
              value: "true"
            - name: DB_USERNAME
              value: "ultrahpm_user"
            - name: DB_PASSWORD
              value: "ultrahpm_password"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres:5432/ultrahpm_notification"
            - name: SPRING_R2DBC_URL
              value: "r2dbc:postgresql://postgres:5432/ultrahpm_notification"
            - name: REDIS_HOST
              value: "redis"
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: "kafka:9092"
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
spec:
  selector:
    app: notification-service
  ports:
    - protocol: TCP
      port: 8205
      targetPort: 8205
